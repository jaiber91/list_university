name: flutter_university
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  domain:
    path: ./lib/src/business/domain
  di:
    path: ./lib/src/di
  
  flutter_native_splash: ^2.4.1
  get_it: ^8.0.3
  go_router: ^14.8.1
  flutter_riverpod: ^2.6.1
  url_launcher: ^6.3.1
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
